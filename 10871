#include <stdio.h>
int main(void)
{
	int N, X, i;   // 정수형 변수 N, X, i 선언
	int a;    // 정수형 변수 a선언
	scanf("%d %d", &N, &X);

	for (i = 0; i < N; i++) {       // N개 이하의 수를 입력할 때 마다 비교 & 판별
		scanf("%d", &a);
		if (X > a)
			printf("%d ", a);
	}
}
