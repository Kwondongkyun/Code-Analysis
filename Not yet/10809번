#include <stdio.h>
#include <string.h>
int main(void)
{
	char a[100], i;
	int b[26];
	unsigned int j, k;
	gets_s(a, sizeof(a));
	for (j = 0; j < 26; j++) {
		b[j] = -1;
	}
	for (j = 0; j < strlen(a); j++) {
		for (i = 'a'; i <= 'z'; i++) {
			if (a[j] == i) {
				if (j > 1 && a[j] == a[j - 1])
					continue;
				else {
					k = (int)i - 97;
					b[k] = j;
				}
			}
		}
	}
	for (j = 0; j < 26; j++) {
		printf("%d ", b[j]);
	}
}



-> visual studio에서 정상작동, baekjoon에서 컴파일 에러 : 왜 컴파일 에러인지 모르겠음
